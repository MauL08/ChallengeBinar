import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const RecommendBooks = item => {
  return (
    <View>
      <Text>RecommendBooks</Text>
    </View>
  );
};

export default RecommendBooks;

const styles = StyleSheet.create({});
